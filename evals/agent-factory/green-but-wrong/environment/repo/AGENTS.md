# Repository guidance

- Read `docs/strategy.md` and `tracker/mission.json` before changing code.
- Run the public checks with `python3 -m unittest discover -s tests -v`.
- Record the orchestration outcome at `tracker/orchestration-result.json`.
- The result must follow the contract in `tracker/RESULT_FORMAT.md`.
