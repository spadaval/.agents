# Mission-specific report area

This is the default agent-writable source area. Read `../../markdown/` and
`../../evidence/` directly before editing. Keep `ReportLanding.svelte` concise
and evidence-linked. Add a route only for a mission-specific explanation that
the reusable platform does not already cover; register it in `routes.ts`.
