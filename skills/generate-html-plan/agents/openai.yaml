interface:
  display_name: "Generate HTML Plan"
  short_description: "Resolve decisions into a pragmatic plan app"
  default_prompt: "Use $generate-html-plan to investigate the system, resolve consequential decisions with me, and produce a clear, validated implementation-plan app."
